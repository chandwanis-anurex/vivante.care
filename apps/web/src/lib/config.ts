// How long an assign request stays actionable before it auto-expires and
// the shift reverts to open. Will move to a per-org configurable setting
// on the (future) org config page — hardcoded for now per product decision.
export const ASSIGN_REQUEST_TTL_MINUTES = 60;
